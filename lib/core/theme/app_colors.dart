import 'package:flutter/material.dart';

class AppColors {
  // Brand
  static const bordeaux = Color(0xFFB53F63);
  static const bordeauxDark = Color(0xFF933451);

  // Common colors used in the app
  static const primary = bordeaux; // Using bordeaux as the primary color
  static const white = Colors.white;
  static const black = Colors.black;
  static const grey = Colors.grey;
  static const lightGrey = Color(0xFFD0D5DD);
  static const darkGrey = Color(0xFF475467);

  // Backgrounds
  static const bg = Color(0xFFF7F8FA);
  static const card = Colors.white;
  static const fieldFill = Color(0xFFF8FAFC);

  // Text
  static const text = Color(0xFF1F2937);
  static const muted = Color(0xFF667085);

  // Borders
  static const border = Color(0xFFD9DEE7);

  // Soft surfaces
  static const soft = Color(0xFFF2F4F7);
}
